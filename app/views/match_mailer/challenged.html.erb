<p>Hi <%= @user.username.presence || @user.display_name %>,</p>
<p><strong><%= @challenger.username %></strong> has challenged you to a game of Cyvasse.</p>
<p><a href="<%= @url %>">Accept the challenge and set up your army</a></p>
<p>The challenge stays open for seven days<%= ", until #{@deadline.utc.strftime('%B %-d at %H:%M UTC')}" if @deadline %>.</p>
